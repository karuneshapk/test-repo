import { defineMessages } from 'react-intl'

export default defineMessages({

  selectApplication: {
    id: 'header.menu.title',
    description: 'title for self service menu',
    defaultMessage: 'Select application'
  }

})
