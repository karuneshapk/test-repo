import { defineMessages } from 'react-intl'

export default defineMessages({

  initErrorText: {
    id: 'addressAutoComplete.init.error.text',
    description: 'Google auto complete component failed on initialization phase',
    defaultMessage: 'Google auto complete module not properly initialized.'
  }

})
